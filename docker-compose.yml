version: '3.8'

services:
  kolabpad:
    build: .
    ports:
      - "3030:3030"
    environment:
      - PORT=3030
      - EXPIRY_DAYS=7
      - SQLITE_URI=/data/kolabpad.db
    volumes:
      - ./data:/data
    restart: unless-stopped
